#include <stdio.h>
/**
 * main -assign a random nuber to n and print if sigle digit of base 10
 * Return Always (0)
 */
int main(void)
{
	int n;

	for (n < 10; n = 0; n++)
	{
		printf("%d", n);
	}
	printf("'/n");
	return (0);
}
