#include <stdio.h>
/**
 * main - all blocks included in main
 * Description: shows all numbers of base 10, starting from 0.
 * Return: int 0
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%i", i);
	}
	putchar('\n');

	return (0);
}
