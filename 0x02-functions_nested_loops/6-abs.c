#include "main.h"
/**
 * _abs - prints absoulte values of int.
 * @n: the integer in the argument.
 * Resturn: The absoulte value
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
